<div class="row"></div>
<div class="container">
<div class="sixteen columns">
<h1>Coverage Reportr</h1>
<h2>Serving Coverage Reports with Ease and Grace</h2>

<ul>
  <% @users.each do |user| %>
  <li>
    <a href="/users/<%= user.id %>"> <%# can this path be written as "users_path(user)"? %>
      <%= user.username %>
    </a>
  </li>
  <% end %>
</ul>

<%= form_tag(new_user_path, method: "get") do %>
<%= submit_tag "Create Account" %>
<% end %>
</div>
</div>
